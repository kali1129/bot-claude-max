# Frontend env. Copy to .env and fill in.

# Backend API URL (must include scheme, no trailing slash).
REACT_APP_BACKEND_URL=http://localhost:8000

# Bearer token to send on write requests. Must match backend DASHBOARD_TOKEN. Empty = none.
REACT_APP_DASHBOARD_TOKEN=
